---
title: "Reports"
sidebarTitle: "Reports"
lang: "en"
---

This page explains Payments report types, the shared inputs used to define report scope, and where to generate or manage reports.

## Generate report scope

When you generate a report, you define the report scope with these shared inputs:

- `start_time`: Start of the reporting time range
- `end_time`: End of the reporting time range
- `report_export_format`: Export format for the generated file
- `report_types`: One or more report types to generate
- `token_ids`: Optional token filter for narrowing the report content

Use these inputs to define the time range, output format, and report scope. For detailed request and response mechanics, see the related API operations below.

## Report types and when to use them

The current Payments documentation confirms the following report types:

| `report_type` | Purpose | Common use case |
| --- | --- | --- |
| `Order` | Export summary data for pay-in orders. | Reconcile order collections and review order-level pay-in activity. |
| `OrderTransaction` | Export transaction data related to pay-in orders. | Review the transaction records associated with order-mode payments. |
| `TopUpTransaction` | Export transaction data for top-up activity. | Reconcile top-up receipts and review top-up transaction records. |
| `PayinWeeklyStatement` | Export a weekly pay-in statement covering order mode and top-up mode. | Review weekly pay-in activity across your pay-in channels. |
| `PayinDailyStatement` | Export a daily pay-in statement covering order mode and top-up mode. | Review daily pay-in activity across your pay-in channels. |
| `CryptoPayout` | Export transaction data for crypto payouts. | Reconcile crypto payout execution and review payout records. |
| `OffRamp` | Export transaction data for fiat off-ramp payouts. | Reconcile fiat payout execution and review off-ramp records. |
| `Refund` | Export transaction data for refunds. | Reconcile refund activity and review refund records. |
| `PayoutWeeklyStatement` | Export a weekly payout statement covering crypto payouts, fiat off-ramps, and refunds. | Review weekly payout activity across payout channels. |
| `PayoutDailyStatement` | Export a daily payout statement covering crypto payouts, fiat off-ramps, and refunds. | Review daily payout activity across payout channels. |
| `PayinCommissionFee` | Export pay-in commission fee data. | Reconcile fees charged for pay-in activity. |
| `PayoutCommissionFee` | Export payout commission fee data. | Reconcile fees charged for payout activity. |
| `BalanceChange` | Export account balance change data. | Review balance movements for reconciliation and account analysis. |
| `Summary` | Export summary data that combines pay-ins, payouts, and commission fees. | Review an aggregated reconciliation view across core payment activity. |
| `BulkSend` | Export transaction data for Bulk Send activity. | Reconcile batch payout execution and review Bulk Send records. |

Different report types cover different business scopes, so the exported columns can differ by `report_type`. Some report types also generate multiple sheets in the exported file.

## Exported fields by report type

Exported fields vary by `report_type`. The following sections list the exported fields confirmed for each report type.

### `Order`

Each row represents one pay-in order.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Order row creation time. |
| `order_id` | Order ID | Unique identifier of the order. |
| `payment_type` | Payment mode | Payment mode for the order row. |
| `order_status` | Order status | Current order status. |
| `order_tag` | Order tag | Order tag exported for the row. |
| `merchant_name` | Merchant name | Merchant name associated with the order. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the order. |
| `token` | Token | Token in the report row. |
| `network` | Network | Network in the report row. |
| `paid_amount` | Paid amount | Amount recorded as paid. |
| `payable_amount` | Payable amount | Amount expected for payment. |
| `product_order_amount` | Product order amount | Product order amount in the row. |
| `developer_fee` | Developer fee | Developer fee in the row. |
| `currency_pair` | Currency pair | Currency pair for the row. |
| `exchange_rate` | Exchange rate | Exchange rate in the row. |
| `wallet_name` | Wallet name | Wallet name in the row. |
| `wallet_type` | Wallet type | Wallet type in the row. |
| `updated_time` | Update time | Latest update time in the row. |
| `expiration_time` | Expiration time | Expiration time for the order row. |

### `OrderTransaction`

Each row represents one transaction associated with order-mode pay-ins.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Transaction row creation time. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the transaction. |
| `order_id` | Order ID | Related order identifier. |
| `order_status` | Order status | Related order status in the row. |
| `transaction_id` | Transaction ID | Unique transaction identifier. |
| `transaction_status` | Transaction status | Current transaction status. |
| `transaction_amount` | Transaction amount | Amount in the transaction row. |
| `from_address` | From address | Source address in the row. |
| `to_address` | To address | Destination address in the row. |
| `txn_hash` | Transaction hash | On-chain transaction hash. |
| `token` | Token | Token in the transaction row. |
| `network` | Network | Network in the transaction row. |
| `developer_fee` | Developer fee | Developer fee in the transaction row. |

### `TopUpTransaction`

Each row represents one top-up transaction.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Transaction row creation time. |
| `payment_type` | Payment mode | Payment mode for the top-up row. |
| `transaction_status` | Transaction status | Current transaction status. |
| `payer_id` | Payer ID | Payer identifier in the row. |
| `custom_payer_id` | Custom payer ID | Custom payer identifier in the row. |
| `received_amount` | Received amount | Amount recorded as received. |
| `from_address` | From address | Source address in the row. |
| `to_address` | To address | Destination address in the row. |
| `transaction_id` | Transaction ID | Unique transaction identifier. |
| `txn_hash` | Transaction hash | On-chain transaction hash. |
| `merchant_name` | Merchant name | Merchant name associated with the row. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the row. |
| `token` | Token | Token in the row. |
| `network` | Network | Network in the row. |
| `developer_fee` | Developer fee | Developer fee in the row. |
| `wallet_name` | Wallet name | Wallet name in the row. |
| `wallet_type` | Wallet type | Wallet type in the row. |
| `updated_time` | Update time | Latest update time in the row. |

### `PayinWeeklyStatement`

`PayinWeeklyStatement` and `PayinDailyStatement` share the same exported columns. Each row represents one statement entry for the selected period.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `period` | Period | Statement period in the row. |
| `token` | Token | Token covered by the statement row. |
| `transaction_number` | Transaction number | Number of transactions in the row. |
| `total_payin_amount` | Total pay-in amount | Total pay-in amount in the row. |
| `order_transaction_number` | Order transaction number | Number of order transactions in the row. |
| `total_order_amount` | Total order amount | Total order amount in the row. |
| `topup_transaction_number` | Top-up transaction number | Number of top-up transactions in the row. |
| `total_topup_amount` | Total top-up amount | Total top-up amount in the row. |
| `unexpected_transaction_number` | Unexpected transaction number | Number of unexpected transactions in the row. |
| `total_unexpected_amount` | Total unexpected amount | Total unexpected amount in the row. |
| `total_payin_commission_fee` | Total pay-in commission fee | Total pay-in commission fee in the row. |

### `PayinDailyStatement`

`PayinDailyStatement` shares the same columns as `PayinWeeklyStatement`.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `period` | Period | Statement period in the row. |
| `token` | Token | Token covered by the statement row. |
| `transaction_number` | Transaction number | Number of transactions in the row. |
| `total_payin_amount` | Total pay-in amount | Total pay-in amount in the row. |
| `order_transaction_number` | Order transaction number | Number of order transactions in the row. |
| `total_order_amount` | Total order amount | Total order amount in the row. |
| `topup_transaction_number` | Top-up transaction number | Number of top-up transactions in the row. |
| `total_topup_amount` | Total top-up amount | Total top-up amount in the row. |
| `unexpected_transaction_number` | Unexpected transaction number | Number of unexpected transactions in the row. |
| `total_unexpected_amount` | Total unexpected amount | Total unexpected amount in the row. |
| `total_payin_commission_fee` | Total pay-in commission fee | Total pay-in commission fee in the row. |

### `CryptoPayout`

The generated file can include separate sheets for standard crypto payouts and bridge-assisted payouts.

#### Sheet: `payout crypto`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Payout row creation time. |
| `payout_id` | Payout ID | Unique payout identifier. |
| `payment_type` | Payment mode | Payment mode for the payout row. |
| `initiator` | Initiator | Initiator in the row. |
| `payout_status` | Payout status | Current payout status. |
| `balance_source` | Balance source | Balance source in the row. |
| `network` | Network | Network in the row. |
| `token` | Token | Token in the row. |
| `to_address` | To address | Destination address in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `actual_payout_amount` | Actual payout amount | Actual payout amount in the row. |
| `merchant_names` | Merchant names | Merchant names included in the row. |
| `merchant_ids` | Merchant IDs | Merchant IDs included in the row. |
| `amounts` | Amounts | Amounts included in the row. |
| `updated_time` | Update time | Latest update time in the row. |

#### Sheet: `payout crypto with bridge`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Payout row creation time. |
| `payout_id` | Payout ID | Unique payout identifier. |
| `payment_type` | Payment mode | Payment mode for the payout row. |
| `initiator` | Initiator | Initiator in the row. |
| `payout_status` | Payout status | Current payout status. |
| `balance_source` | Balance source | Balance source in the row. |
| `original_network` | Original network | Original network in the row. |
| `original_token` | Original token | Original token in the row. |
| `payout_network` | Payout network | Payout network in the row. |
| `payout_token` | Payout token | Payout token in the row. |
| `bridge_fee` | Bridge fee | Bridge fee in the row. |
| `to_address` | To address | Destination address in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `actual_payout_amount` | Actual payout amount | Actual payout amount in the row. |
| `merchant_names` | Merchant names | Merchant names included in the row. |
| `merchant_ids` | Merchant IDs | Merchant IDs included in the row. |
| `amounts` | Amounts | Amounts included in the row. |
| `updated_time` | Update time | Latest update time in the row. |

### `OffRamp`

Each row represents one off-ramp payout record.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Payout row creation time. |
| `payout_id` | Payout ID | Unique payout identifier. |
| `payment_type` | Payment mode | Payment mode for the payout row. |
| `initiator` | Initiator | Initiator in the row. |
| `payout_status` | Payout status | Current payout status. |
| `balance_source` | Balance source | Balance source in the row. |
| `bank_name` | Bank name | Bank name in the row. |
| `bank_account_number` | Bank account number | Bank account number in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `actual_payout_amount` | Actual payout amount | Actual payout amount in the row. |
| `merchant_names` | Merchant names | Merchant names included in the row. |
| `merchant_ids` | Merchant IDs | Merchant IDs included in the row. |
| `amounts` | Amounts | Amounts included in the row. |
| `updated_time` | Update time | Latest update time in the row. |

### `Refund`

Each row represents one refund record.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Refund row creation time. |
| `refund_order_id` | Refund order ID | Unique refund order identifier. |
| `payment_type` | Payment mode | Payment mode for the refund row. |
| `initiator` | Initiator | Initiator in the row. |
| `transaction_status` | Transaction status | Current transaction status in the row. |
| `merchant_name` | Merchant name | Merchant name associated with the row. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the row. |
| `order_id` | Order ID | Related order identifier. |
| `token` | Token | Token in the refund row. |
| `network` | Network | Network in the refund row. |
| `refund_amount` | Refund amount | Refund amount in the row. |
| `to_address` | To address | Destination address in the row. |
| `balance_source` | Balance source | Balance source in the row. |
| `wallet_name` | Wallet name | Wallet name in the row. |
| `wallet_type` | Wallet type | Wallet type in the row. |
| `updated_time` | Update time | Latest update time in the row. |
| `description` | Description | Description in the row. |

### `PayoutWeeklyStatement`

`PayoutWeeklyStatement` and `PayoutDailyStatement` share the same exported columns. Each row represents one statement entry for the selected period.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `period` | Period | Statement period in the row. |
| `token` | Token | Token covered by the statement row. |
| `transaction_number` | Transaction number | Number of transactions in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `withdraw_crypto_transaction_number` | Withdraw crypto transaction number | Number of crypto payout transactions in the row. |
| `total_withdraw_crypto_amount` | Total withdraw crypto amount | Total crypto payout amount in the row. |
| `off_ramp_transaction_number` | Off-ramp transaction number | Number of off-ramp transactions in the row. |
| `total_off_ramp_amount` | Total off-ramp amount | Total off-ramp amount in the row. |
| `refund_transaction_number` | Refund transaction number | Number of refund transactions in the row. |
| `total_refund_amount` | Total refund amount | Total refund amount in the row. |
| `unexpected_transaction_number` | Unexpected transaction number | Number of unexpected transactions in the row. |
| `total_unexpected_amount` | Total unexpected amount | Total unexpected amount in the row. |
| `total_payout_commission_fee` | Total payout commission fee | Total payout commission fee in the row. |

### `PayoutDailyStatement`

`PayoutDailyStatement` shares the same columns as `PayoutWeeklyStatement`.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `period` | Period | Statement period in the row. |
| `token` | Token | Token covered by the statement row. |
| `transaction_number` | Transaction number | Number of transactions in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `withdraw_crypto_transaction_number` | Withdraw crypto transaction number | Number of crypto payout transactions in the row. |
| `total_withdraw_crypto_amount` | Total withdraw crypto amount | Total crypto payout amount in the row. |
| `off_ramp_transaction_number` | Off-ramp transaction number | Number of off-ramp transactions in the row. |
| `total_off_ramp_amount` | Total off-ramp amount | Total off-ramp amount in the row. |
| `refund_transaction_number` | Refund transaction number | Number of refund transactions in the row. |
| `total_refund_amount` | Total refund amount | Total refund amount in the row. |
| `unexpected_transaction_number` | Unexpected transaction number | Number of unexpected transactions in the row. |
| `total_unexpected_amount` | Total unexpected amount | Total unexpected amount in the row. |
| `total_payout_commission_fee` | Total payout commission fee | Total payout commission fee in the row. |

### `PayinCommissionFee`

Each row represents one commission fee record related to pay-in activity.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `merchant_id` | Merchant ID | Merchant identifier in the row. |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `source` | Source | Source in the row. |
| `billing_date` | Billing date | Billing date in the row. |
| `transaction_amount` | Transaction amount | Transaction amount in the row. |
| `fee_rate` | Fee rate | Fee rate in the row. |
| `fee_amount` | Fee amount | Fee amount in the row. |
| `fee_type` | Fee type | Fee type in the row. |

### `PayoutCommissionFee`

Each row represents one commission fee record related to payout activity.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `merchant_id` | Merchant ID | Merchant identifier in the row. |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `source` | Source | Source in the row. |
| `to_address` | To address | Destination address in the row. |
| `billing_date` | Billing date | Billing date in the row. |
| `transaction_amount` | Transaction amount | Transaction amount in the row. |
| `fee_rate` | Fee rate | Fee rate in the row. |
| `fee_amount` | Fee amount | Fee amount in the row. |
| `fee_type` | Fee type | Fee type in the row. |

### `BalanceChange`

Each row represents one balance change record.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `balance_type` | Balance type | Balance type in the row. |
| `merchant_name` | Merchant name | Merchant name associated with the row. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the row. |
| `token` | Token | Token in the row. |
| `network` | Network | Network in the row. |
| `amount_raw` | Amount | Amount in the row. |
| `balance_change_type` | Balance change type | Balance change type in the row. |
| `source` | Source | Source in the row. |
| `amount_change` | Amount change | Amount change in the row. |
| `updated_balance` | Updated balance | Updated balance in the row. |
| `business_date` | Business date | Business date in the row. |

### `Summary`

The generated file contains four sheets: `pay-in`, `pay-out`, `commission-fee`, and `cobo-bill`.

#### Sheet: `pay-in`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `transaction_id` | Transaction ID | Unique transaction identifier. |
| `created_time` | Record creation time | Transaction row creation time. |
| `transaction_status` | Transaction status | Current transaction status. |
| `merchant_name` | Merchant name | Merchant name associated with the row. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the row. |
| `payer_id` | Payer ID | Payer identifier in the row. |
| `custom_payer_id` | Custom payer ID | Custom payer identifier in the row. |
| `order_id` | Order ID | Related order identifier. |
| `psp_order_code` | PSP order code | PSP order code in the row. |
| `from_address` | From address | Source address in the row. |
| `to_address` | To address | Destination address in the row. |
| `txn_hash` | Transaction hash | On-chain transaction hash. |
| `token` | Token | Token in the row. |
| `network` | Network | Network in the row. |
| `transaction_amount` | Transaction amount | Transaction amount in the row. |
| `developer_fee` | Developer fee | Developer fee in the row. |
| `wallet_name` | Wallet name | Wallet name in the row. |
| `wallet_type` | Wallet type | Wallet type in the row. |
| `updated_time` | Update time | Latest update time in the row. |

#### Sheet: `pay-out`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `created_time` | Record creation time | Transaction row creation time. |
| `transaction_id` | Transaction ID | Unique transaction identifier. |
| `transaction_status` | Transaction status | Current transaction status. |
| `merchant_name` | Merchant name | Merchant name associated with the row. |
| `merchant_id` | Merchant ID | Merchant identifier associated with the row. |
| `from_address` | From address | Source address in the row. |
| `to_address` | To address | Destination address in the row. |
| `txn_hash` | Transaction hash | On-chain transaction hash. |
| `token` | Token | Token in the row. |
| `network` | Network | Network in the row. |
| `transaction_amount` | Transaction amount | Transaction amount in the row. |
| `wallet_name` | Wallet name | Wallet name in the row. |
| `wallet_type` | Wallet type | Wallet type in the row. |
| `bulk_send_id` | Bulk send ID | Bulk send identifier in the row. |
| `bulk_send_item_id` | Bulk send item ID | Bulk send item identifier in the row. |
| `bulk_send_remark` | Bulk send remark | Bulk send remark in the row. |
| `bulk_send_item_remark` | Bulk send item remark | Bulk send item remark in the row. |
| `updated_time` | Update time | Latest update time in the row. |

#### Sheet: `commission-fee`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `merchant_id` | Merchant ID | Merchant identifier in the row. |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `source` | Source | Source in the row. |
| `to_address` | To address | Destination address in the row. |
| `billing_date` | Billing date | Billing date in the row. |
| `transaction_amount` | Transaction amount | Transaction amount in the row. |
| `fee_rate` | Fee rate | Fee rate in the row. |
| `fee_amount` | Fee amount | Fee amount in the row. |
| `fee_type` | Fee type | Fee type in the row. |

#### Sheet: `cobo-bill`

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `transaction_type` | Transaction type | Transaction type in the row. |
| `source` | Source | Source in the row. |
| `billing_date` | Billing date | Billing date in the row. |
| `fee_amount` | Fee amount | Fee amount in the row. |

### `BulkSend`

Each row represents one Bulk Send record.

| Field | Exported label or meaning | Note |
| --- | --- | --- |
| `created_time` | Record creation time | Bulk Send row creation time. |
| `bulk_send_id` | Bulk send ID | Bulk Send identifier in the row. |
| `item_id` | Item ID | Item identifier in the row. |
| `transaction_id` | Transaction ID | Transaction identifier in the row. |
| `payment_type` | Payment mode | Payment mode for the row. |
| `bulk_send_execution_mode` | Bulk send execution mode | Execution mode in the row. |
| `bulk_send_item_status` | Bulk send item status | Item status in the row. |
| `balance_source` | Balance source | Balance source in the row. |
| `network` | Network | Network in the row. |
| `token` | Token | Token in the row. |
| `to_address` | To address | Destination address in the row. |
| `total_payout_amount` | Total payout amount | Total payout amount in the row. |
| `actual_payout_amount` | Actual payout amount | Actual payout amount in the row. |
| `merchant_names` | Merchant names | Merchant names included in the row. |
| `merchant_ids` | Merchant IDs | Merchant IDs included in the row. |
| `updated_time` | Update time | Latest update time in the row. |

After you confirm the fields you need, use the related API operations below to generate reports or list generated reports.

## Related API operations

- [Generate reports](/payments/en/api-references/payment/generate-reports)
- [List all reports](/payments/en/api-references/payment/list-all-reports)
