---
title: "错误码与状态码"
lang: "zh-hans"
---

本文介绍了在使用 Cobo Payments API 时可能遇到的常见错误码和 HTTP 状态码，以及如何解决这些错误。

当请求失败时，响应体中包含 `error_code` 字段（整数）、描述问题的 `error_message` 字段，以及可用于在 Cobo 日志中追踪请求的 `error_id` 字段。

### 错误码

#### 通用 API 错误

| 错误码 | HTTP 状态码 | 描述 | 解决方案 |
|---|---|---|---|
| 2006 | 400 | 一个或多个参数格式无效或包含不支持的值。 | 根据 [API 参考](/payments/cn/api-references/overview)验证请求体。检查参数类型、枚举值以及所有必填字段。`error_message` 字段通常会指明验证失败的具体参数。 |
| 2010 | 429 | 您已超出 API 访问频率限制。 | 降低请求频率。在重试前实施指数退避加抖动策略。 |
| 2001 | 400 | 执行此操作前需要完成 MFA 验证。 | 在 Cobo Portal 中完成 MFA 验证，然后重试请求。 |
| 2024 | 401 | API Key 认证失败。 | <ul><li>如果 `error_message` 提示该 API Key 未注册到当前环境，请切换到正确的 API 域名，并使用对应环境的 API Key。</li><li>如果 `error_message` 提示该 API Key 尚未激活，请完成所需审批流程，使 API Key 进入活跃状态。</li><li>如果 `error_message` 提到 IP 白名单，请将您服务器的出口 IP 地址加入 Cobo Portal 中该 API Key 的白名单。</li><li>如果是签名校验失败，请检查签名使用的请求路径和请求体是否正确，以及 nonce 是否与 `Biz-Api-Nonce` 请求头一致。</li></ul>详情请参见[发送首个请求](/payments/cn/developer-tools/get-started-with-api)。 |
| 2025, 4001 | 403 | 禁止访问请求的资源。 | <ul><li>检查与您的 API Key 关联的权限和 scope。详情请参见[角色与权限](/payments/cn/guides/roles-and-permissions)。</li><li>如果错误表示资源不属于当前组织，请确认您访问的资源与 API Key 属于同一组织。</li><li>请确认您使用的是正确的环境与 API Key 组合。</li></ul> |
| 4002 | 400 | 您的组织状态不允许执行此操作。您的开发者账户可能尚未完成激活或处于审核中。 | 确认您的开发者入驻流程已全部完成。如问题持续，您的账户可能受到限制——请查看 Cobo Portal 中的相关通知。 |

#### 支付资源错误

支付相关的验证失败——例如资源未找到或字段值被拒绝——均返回 `"error_code": 2006`。使用响应中的 `error_message` 字段来识别具体原因。

| 场景 | `error_message` 示例 | 解决方案 |
|---|---|---|
| 商户、钱包或交易未找到 | 例如 `Wallet not found.`、`Transaction {transaction_id} not found.`、`Request ID {request_id} not found.` | 验证相关 ID 是否正确，且属于您的组织。 |
| Token 值被拒绝 | 例如 `Token not found.`、`token_id:{token_id} not found` | 检查 token ID 是否正确，并确认该 endpoint 支持该 token。 |
| 参数值无效 | 例如 `Invalid parameter: {param_name}.`、`Invalid parameter: {param_name}({param_value}).` | 对照 API 参考检查请求，并修正 `error_message` 中指出的字段值。 |
| 金额或地址无效 | 例如 `Error: Invalid amount. Please enter a valid numerical value.`、`Invalid withdrawal address.` | 使用有效的数值金额，并提供与指定 token 和链匹配的目标地址。 |

### HTTP 状态码

| 状态码 | 描述 | 解决方案 |
|---|---|---|
| 200 | 成功。 | 不适用 |
| 400 | 错误请求。 | 检查请求参数。 |
| 401 | 未经授权。 | 检查 API Key 是否与当前环境匹配、API 签名是否正确，以及时间戳或 nonce 是否有效并与签名请求保持一致。 |
| 403 | 禁止访问。 | 先检查响应体中的业务错误码（例如 `4001`），再进一步核对权限、scope、组织归属，以及是否缺少相关功能开通。 |
| 404 | 未找到。 | 检查请求 URL。 |
| 405 | 方法不允许。 | 使用支持的 HTTP 方法。 |
| 406 | 不可接受。 | 确保请求内容格式为 JSON。 |
| 429 | 请求过多。 | 降低请求频率并稍后重试。 |
| 500 | 内部服务器错误。此错误可能由多个问题引起。 | 检查您的服务器配置并稍后重试。如果问题持续，请结合 `error_id` 联系 Cobo 支持排查。 |
| 502 | 错误网关。 | 检查连接并稍后重试。 |
| 503 | 服务不可用。 | 稍后重试。 |
